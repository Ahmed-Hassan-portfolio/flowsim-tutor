# Core MCP + RAG pipeline
mcp[cli]>=1.0,<2.0
chromadb>=1.3,<2.0
sentence-transformers>=3.3,<4.0
rank-bm25>=0.2.2
pydantic>=2.0

# Chat UI (optional but enabled by default in `pip install -e .[chat]`)
chainlit>=1.0
anthropic>=0.40
openai>=1.0
python-dotenv>=1.0
